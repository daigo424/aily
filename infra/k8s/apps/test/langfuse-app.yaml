apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: langfuse
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: https://langfuse.github.io/langfuse-k8s
    chart: langfuse
    targetRevision: "1.*"
    helm:
      valuesObject:
        langfuse:
          salt:
            secretKeyRef:
              name: langfuse-secret
              key: SALT
          nextauth:
            url: "http://localhost:13001"
            secret:
              secretKeyRef:
                name: langfuse-secret
                key: NEXTAUTH_SECRET
          additionalEnv:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: langfuse-secret
                  key: DATABASE_URL
            - name: REDIS_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: langfuse-secret
                  key: REDIS_CONNECTION_STRING
          tolerations:
            - key: workload
              operator: Equal
              value: "true"
              effect: NoSchedule
          serviceAccount:
            annotations:
              eks.amazonaws.com/role-arn: arn:aws:iam::815935788564:role/aily-test-langfuse-role
        postgresql:
          enabled: false
        redis:
          enabled: false
        clickhouse:
          enabled: false
          host: clickhouse-langfuse.clickhouse.svc.cluster.local
          clusterName: langfuse
          auth:
            username: langfuse
            existingSecret: langfuse-secret
            existingSecretKey: CLICKHOUSE_PASSWORD
        s3:
          enabled: false
          region: us-west-2
          bucket: aily-test-ml-data
          mediaUpload:
            enabled: true
            prefix: langfuse/
  destination:
    server: https://kubernetes.default.svc
    namespace: langfuse
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
