apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-workflows
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  source:
    repoURL: https://argoproj.github.io/argo-helm
    chart: argo-workflows
    targetRevision: "1.0.x"
    helm:
      valuesObject:
        controller:
          tolerations:
            - key: role
              operator: Equal
              value: workload
              effect: NoSchedule
          nodeSelector:
            role: workload
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
          workflowDefaults:
            spec:
              archiveLogs: true
        server:
          tolerations:
            - key: role
              operator: Equal
              value: workload
              effect: NoSchedule
          nodeSelector:
            role: workload
          authModes:
            - server
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
          serviceAccount:
            create: true
            name: argo-workflows-server
            annotations:
              eks.amazonaws.com/role-arn: arn:aws:iam::815935788564:role/aily-test-argo-workflows-server-role
        workflow:
          serviceAccount:
            create: true
            name: argo-workflows
  destination:
    server: https://kubernetes.default.svc
    namespace: argo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
