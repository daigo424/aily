apiVersion: apps/v1
kind: Deployment
metadata:
  name: aily-api
  namespace: aily
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aily-api
  template:
    metadata:
      labels:
        app: aily-api
    spec:
      serviceAccountName: aily-api
      containers:
        - name: api
          image: REPLACE_WITH_ECR_AILY_API_URL
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: aily-secret
          env:
            - name: PORT
              value: "8000"
            - name: MLFLOW_TRACKING_URI
              value: http://mlflow.mlflow.svc.cluster.local:5000
            - name: REDIS_HOST
              value: redis-master.redis.svc.cluster.local
            - name: LLM_BASE_URL
              value: http://vllm.aily-ml.svc.cluster.local:8000/v1
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
