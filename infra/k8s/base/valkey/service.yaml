apiVersion: v1
kind: Service
metadata:
  name: redis-primary-headless
  namespace: redis
spec:
  clusterIP: None
  ports:
    - port: 6379
      targetPort: valkey
  selector:
    app: valkey
---
apiVersion: v1
kind: Service
metadata:
  name: redis-primary
  namespace: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: valkey
  selector:
    app: valkey
